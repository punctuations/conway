[package]
name = "conway"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive", "wrap_help"] }
crossterm = { version = "0.29.0", features = ["use-dev-tty"] }
